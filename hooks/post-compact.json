{
  "kind": "hook",
  "intent": "post-compact",
  "name": "post-compact",
  "description": "Re-surface the anchors captured before compaction (decisions, test names, task anchors) once Claude has produced the compacted summary, so the agent resumes with them in view.",
  "visibility": "internal",
  "platforms": ["claude"],
  "trigger": {
    "matcher": "manual|auto"
  },
  "x-claude": {
    "event": "PostCompact",
    "matcher": "manual|auto",
    "dispatch": "command",
    "command": ".claude-plugin/hooks/post-compact.sh"
  }
}
