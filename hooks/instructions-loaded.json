{
  "kind": "hook",
  "intent": "instructions-loaded",
  "name": "instructions-loaded",
  "description": "Observe each CLAUDE.md / .claude/rules file as it loads into context and report a running count plus any rules that loaded without a path-glob match, for the aegis-doctor health check.",
  "visibility": "internal",
  "platforms": ["claude"],
  "x-claude": {
    "event": "InstructionsLoaded",
    "dispatch": "command",
    "command": ".claude-plugin/hooks/instructions-loaded.sh"
  }
}
