{
  "kind": "hook",
  "intent": "cwd-changed",
  "name": "cwd-changed",
  "description": "When the working directory changes, note which language overlay is active for the new directory so the agent reaches for the right developer skill. Advisory only.",
  "visibility": "internal",
  "platforms": ["claude"],
  "trigger": {
    "paths": ["**/*.ts", "**/*.tsx", "**/*.py", "**/*.rs", "**/*.go"]
  },
  "x-claude": {
    "event": "CwdChanged",
    "dispatch": "command",
    "command": ".claude-plugin/hooks/cwd-changed.sh"
  }
}
