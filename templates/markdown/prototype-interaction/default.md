# {{ slot.title }}

> **Prototype / interaction** — a throwaway sketch so the team can judge the *feel*
> of an interaction before it is built for real.

## The interaction

{{ slot.stage }}

## What you're feeling

The behaviour decisions baked into the prototype, so they can be pushed back on:

{{ slot.behaviorNotes }}

## Open questions

<!-- Render one list item per open question. -->

- {{ slot.openQuestions }}
